apiVersion: v1
kind: Service
metadata:
  name: mcp-rancher
  namespace: mcp-rancher
spec:
  selector:
    app: mcp-rancher
  ports:
    - name: http
      port: 80
      targetPort: 8765
  type: ClusterIP
